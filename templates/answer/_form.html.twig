

<div>
    {{ form_start(form, {'attr' : {'class': 'answer--form'}} ) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>

    <div class="form--answer">
        <div class="col">
            {{ form_row(form.content, {'attr' : {'placeholder': 'Ecrire une réponse'}} ) }}
        </div> 
       <button class="d-flex justify-content-end form--button" type="submit"> Envoyer </button> 
    </div>
   
    {{ form_end(form) }}
</div>